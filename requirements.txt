httpx>=0.27
openai>=1.60
openpyxl>=3.1
pydantic>=2.9
python-dotenv>=1.0
rich>=13.9
pytest>=8.3
pytest-asyncio>=0.24
sarvamai>=0.1.30
# The sound card, in both directions. PortAudio bindings, prebuilt wheels on every
# platform; the only dependency that touches hardware. Needed for --voice and for
# streaming playback, not for --silent runs.
sounddevice>=0.5

# The browser console (app.py). Gradio 6 moved theme and css from Blocks() to launch();
# its streaming audio output is AAC in an ADTS container, which needs ffmpeg and ffprobe
# on PATH; without them the console falls back to a sentence at a time on its own.
gradio>=6.0
